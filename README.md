# hon
⛩ Book reading planner

## Development

This repo includes a top-level `Justfile` for common tasks:

- `just install` installs backend and frontend dependencies
- `just build` builds the backend package and frontend app
- `just run` starts backend and frontend together
